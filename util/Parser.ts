const Parser = {
    cache: ParserMetaData = ParserMetaData(
        // empthy respond thatshould work at place holder
        FoolishRespond();
    )
    httpParser(httpRequest: Deno.Conn): FoolishRequest {
        return;
    }
}

